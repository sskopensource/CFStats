<Application x:Class="SampleUi.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:SampleUi"
             StartupUri="Window2.xaml">
    <Application.Resources>
        
        <!--Resources-->
        <SolidColorBrush Color="Red" x:Key="RedBrush"/>
        <SolidColorBrush Color="Blue" x:Key="BlueBrush"/>
        
        <!--Styles-->
        <Style TargetType="Button" x:Key="SampleStyle">
            <Setter Property="Background" Value="Teal"/>
        </Style>
        
        <!--StyleInheritance-->
        <Style BasedOn="{StaticResource SampleStyle}" TargetType="Button" x:Key="SampleStyleInheritance">
            <Setter Property="FontWeight" Value="ExtraBold"/>
        </Style>
        
        <!--Triggers-->
        <Style BasedOn="{StaticResource SampleStyle}" TargetType="Button" x:Key="Trigger">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="FontWeight" Value="ExtraBold"/>
                 </Trigger>
            </Style.Triggers>
        </Style>
        
    </Application.Resources>
</Application>
