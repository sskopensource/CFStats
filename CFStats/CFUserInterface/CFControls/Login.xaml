<UserControl x:Class="UserInterface.CFControls.Login"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UserInterface.CFControls" xmlns:VM="clr-namespace:UserInterface"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Canvas Height="40" Width="{Binding ElementName=loginLabel, XPath=Width}" MinWidth="{Binding ElementName=loginLabel, XPath=Width}">
            <Ellipse Width="40" Height="40" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Ellipse.Fill>
                    <ImageBrush ImageSource="{Binding ImgURL ,RelativeSource={RelativeSource AncestorType=UserControl}}" />
                </Ellipse.Fill>
            </Ellipse>
            <Label Canvas.Left="50" x:Name="loginLabel" VerticalAlignment="Center" HorizontalAlignment="Left" FontSize="20" Width="Auto">
                <Binding x:Name="binding" Path="UserName" RelativeSource="{RelativeSource AncestorType={x:Type UserControl}}"/>
            </Label>
        </Canvas>
    </Grid>
</UserControl>
